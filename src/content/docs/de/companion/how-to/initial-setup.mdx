---
title: "Edge-Ersteinrichtung"
description: "Netzwerkeinstellungen eines neuen Edge-Geräts über Crawler.Companion konfigurieren, damit es im Netzwerk erreichbar ist."
sidebar:
  order: 1
---

import { Steps } from '@astrojs/starlight/components';

Ein neues Edge-Gerät ist ab Werk noch nicht im Netzwerk erreichbar. Crawler.Companion erkennt es beim Anschließen automatisch und ermöglicht es, die Netzwerkeinstellungen direkt zu konfigurieren — ohne direkten Zugang zur Web-Oberfläche des Geräts. Die Ersteinrichtung über Companion beschränkt sich auf diesen Schritt; alles Weitere erfolgt anschließend über die Web-Oberfläche des Geräts.

<div class="ht-note-block">
  <p>Die vollständige Geräteeinrichtung (Hub-Verbindung, Sensoren, Konfiguration) erfolgt über die Web-Oberfläche des Edge-Geräts mit dem Einrichtungs-Wizard — nicht über Companion. Sobald die Netzwerkeinstellungen gespeichert sind, den Guide <a href="/de/edge/how-to/initial-setup">Ersteinrichtung (Edge)</a> aufrufen, um den Vorgang abzuschließen.</p>
</div>

Bevor das Gerät genutzt werden kann, benötigt es eine im Netzwerk erreichbare IP-Adresse. Diese Anleitung erklärt, wie die Netzwerkkonfiguration eingestellt wird.

<div class="ht-note-block">
  <p>Crawler.Companion erkennt nur Geräte im gleichen physischen Netzwerk. PC und Edge-Gerät müssen direkt am gleichen Switch oder Kabel angeschlossen sein.</p>
</div>

<div class="ht-phase-steps">

  <div class="ht-phase-label">Gerät finden</div>

  <Steps>

  1. **Companion öffnen**

     Crawler.Companion starten. Es scannt das Netzwerk automatisch. Neue Geräte erscheinen in der Geräteliste mit dem Status **Einrichtung erforderlich**.

     <div class="screenshot-frame">
       <img src="/images/companion/einrichtung-erforderlich.png" alt="Geräteliste mit einem Gerät mit Status Einrichtung erforderlich" />
     </div>

  2. **Gerät auswählen**

     Auf das Gerät klicken, um das Detailpanel zu öffnen.

  </Steps>

  <div class="ht-phase-cont-2">

    <div class="ht-phase-label">Netzwerk konfigurieren</div>

    <Steps>

    1. **Tab „Netzwerkeinstellungen" öffnen**

       Im Detailpanel auf den Tab **Netzwerkeinstellungen** klicken.

    2. **IP-Modus wählen**

       **DHCP** (IP wird automatisch vom Router vergeben) oder **Statische IP** (feste IP-Adresse) auswählen.

    3. **Netzwerkwerte eintragen** *(nur bei Statischer IP)*

       Bei **Statische IP** folgende Felder ausfüllen:

       - **IP-Adresse** — z. B. `192.168.1.50`
       - **Subnetzmaske** — z. B. `255.255.255.0`
       - **Gateway** — z. B. `192.168.1.1`

    4. **Konfiguration speichern**

       Auf **Speichern** klicken. Das Gerät übernimmt die neuen Netzwerkeinstellungen.

    </Steps>

  </div>

</div>

Sobald das Gerät unter der neuen Adresse erreichbar ist, die Web-Oberfläche des Edge-Geräts aufrufen und den Einrichtungs-Wizard durchlaufen — siehe [Ersteinrichtung (Edge)](/de/edge/how-to/initial-setup).
